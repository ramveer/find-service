
export const createOrUpdatePermission = async ({
  deviceId,
  allowedPhone,
  isActive,
}: {
  deviceId: number;
  allowedPhone: string;
  isActive: boolean;
}) => {
  // Logic to create or update permission record in the database
};